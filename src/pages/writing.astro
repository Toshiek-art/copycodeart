---
import BaseLayout from '../layouts/BaseLayout.astro';
import { writings } from '../data/writing';

const items = writings.filter(w => w.status === 'published' && w.visible);
---
<BaseLayout title="Writing — CopyCodeArt">
  <h1 class="text-2xl font-bold mb-6">Writing</h1>
  <ul class="space-y-4">
    {items.map(w => (
      <li>
        <a href={`/writing/${w.slug}`} class="font-semibold">{w.title}</a>
        <p class="text-sm opacity-80">{w.excerpt}</p>
      </li>
    ))}
  </ul>
</BaseLayout>
